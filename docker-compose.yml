services:
  core-service:
    build: .
    image: skillsync/core-service:0.1.0
    env_file: .env
    environment:
      PORT: "5000"
      FIREBASE_REQUIRED: "true"
    ports:
      - "${APP_PORT:-5000}:5000"
