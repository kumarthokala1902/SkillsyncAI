apiVersion: v1
kind: Service
metadata:
  name: {{ include "kumar-chart.fullname" . }}
  labels:
    {{- include "kumar-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kumar-chart.selectorLabels" . | nindent 4 }}
