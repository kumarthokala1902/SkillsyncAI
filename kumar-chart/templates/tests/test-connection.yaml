apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kumar-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kumar-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kumar-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
