apiVersion: v1
kind: Service
metadata:
  name: skillsync-core
  namespace: skillsync
  labels:
    app.kubernetes.io/name: skillsync-core
spec:
  selector:
    app.kubernetes.io/name: skillsync-core
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
